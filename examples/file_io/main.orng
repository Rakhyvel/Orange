fn main() -> ()!() with (core::Writing, core::Allocating, core::Args, core::File_IO) {
    if core::Args.length != 3 {
        try @println("give me a first name and last name!")
        return .err
    }

    let f = core::File_IO.fs.>open("test.txt", .write) orelse {return .err}
    defer f.>close()

    try @write(f as &mut dyn core::Writer, "Hello, {core::Args[1]} {core::Args[2]}!\n")

    .ok
}
