import std::fmt

// The `Args` ability allows functions to access the command-line arguments
fn main() -> ()!() with (core::Writing, core::Args) {
    try @println("given {core::Args.length} args:")

    for i in 0..core::Args.length {
        try @println("arg[{i}]: {core::Args[i]}")
    }

    .ok
}