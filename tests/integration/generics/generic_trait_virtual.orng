// 431
trait Gen_Trait[T] {
    virtual fn get_value(&self) -> T
}

struct Impler {
    value: Int
}

impl Gen_Trait[Int] for Impler {
    virtual fn get_value(&self) -> Int {
        self.value        
    }
}

fn extract_value(u: &dyn Gen_Trait[Int]) -> Int {
    u.>get_value()
}

fn main() -> Int {
    let x = Impler(431)
    extract_value(&x)
}
