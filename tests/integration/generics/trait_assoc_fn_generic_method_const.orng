// 246
trait Scaler { fn scaled[const k: Int](&self) -> Int }

struct Box[T] { v: T }

impl[T] Scaler for Box[T] {
    fn scaled[const k: Int](&self) -> Int {
        _ = self
        k
    }
}

fn main() -> Int {
    let b = Box[Int](123)
    Scaler::scaled[246](&b)
}
