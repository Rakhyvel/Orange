// 5
struct Foo[T, S] {
    x: Int
    y: Int
}

impl[T] for Foo[T, ()] {
    fn make() -> Self { Self(0, 0) }
    fn wrap() -> Self { Foo[T, ()]::make() }
}

fn main() -> Int {
    let f = Foo[String, ()]::wrap()
    f.x
}
