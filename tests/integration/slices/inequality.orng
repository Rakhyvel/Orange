// 533
fn main() -> Int {
    let x: []Int = &[1, 2, 3]
    let y: []Int = &[3, 2, 1]
    check(x, y)
}

fn check(x: []Int, y: []Int) -> Int {
    if x != y {
        533
    } else {
        235
    }
}