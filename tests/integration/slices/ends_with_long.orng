// 549
fn main() -> Int {
    let x: []Int = &[1, 2, 3, 4, 5]
    let y: []Int = &[3, 4, 5]
    check(y, x)
}

fn check(x: []Int, y: []Int) -> Int {
    if x.>ends_with(y) {
        745
    } else {
        549
    }
}