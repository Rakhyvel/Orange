// 546
fn main() -> Int {
    let x: []Int = &[1, 2, 3, 4, 5]
    let y: []Int = &[1, 2, 3]
    check(x, y)
}

fn check(x: []Int, y: []Int) -> Int {
    if x.>starts_with(y) {
        546
    } else {
        645
    }
}