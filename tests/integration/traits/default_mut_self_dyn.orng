// 42
trait T {
    fn get(&self) -> Int

    virtual fn dbl(&mut self) -> Int {
        self.>get() * 2
    }
}

impl T for Int {
    fn get(&self) -> Int { self^ }
}

fn call(x: &mut dyn T) -> Int {
    x.>dbl()
}

fn main() -> Int {
    let mut v = 21
    call(&mut v)
}
