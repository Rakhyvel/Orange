// 590
fn main() -> Int {
    let doer = Doer[Int, My_Context](scale)
    with My_Context(295) {
        doer.f(2)
    }
}

struct Doer[T, context C] {
    f: T -> T with C
}

context My_Context = Int

fn scale(x: Int) -> Int with My_Context { x * My_Context }